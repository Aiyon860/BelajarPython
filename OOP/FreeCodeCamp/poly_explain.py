name = "Daniel"
print(len(name))    # Printing the length of the str

lunch = ["Spicy Chicken", "Soda"]
print(len(lunch))   # Printing the length of elements in list

# That's polymorphism in action, a single function does know
# how to handle different kinds of objects as expected!